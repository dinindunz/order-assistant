{
  "name": "describe_table",
  "description": "Get the schema and structure of a PostgreSQL table. Returns column names, data types, constraints, and indexes for the specified table.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "table_name": {
        "type": "string",
        "description": "The name of the table to describe (e.g., 'products', 'orders')"
      }
    },
    "required": ["table_name"]
  }
}
